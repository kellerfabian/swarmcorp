[project]
name = "swarmcorp"
version = "0.1.0"
description = "Multi-Agent KI-Firma Simulation"
requires-python = ">=3.11"
dependencies = [
    "anthropic",
    "python-dotenv",
    "fastapi",
    "uvicorn[standard]",
    "websockets",
]
